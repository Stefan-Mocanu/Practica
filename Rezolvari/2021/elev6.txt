Subiectul I
1. a
2. d
3. d
4. a
5. b

Subiectul II
1.
	a) 1111121121
	b) 8, 12 
	c)
#include <iostream>
using namespace std;

int main() {
	int x, y, nr = 1;
	if (x > y) {
		x = x + y;
		y = x - y;
		x = x - y;
	}
	for (int i = y; i < x; i--) {
		cout << 1;
		if (nr >= x) cout << 2;
		nr = nr * 3;
		cout << 1;
	}
	return 0;
}
	d)
citește x, y
	(numere naturale nenule)
dacă x>y atunci x <-> y
|O

nr <- 1
i <- y
cât timp i < x execută
| scrie 1
| dacă nr >= x atunci
| | scrie 2
| |O
| nr <- nr*3
| scrie 1
| i <- i - 1
|O

2.
(ciocârlie, mierlă)
(mierlă, cinteză)
3.
fs->a = f->a - 2020
fs->b = f->b - 2021
Subiectul III
1.
function divPrim(int s, int n) {
	for (i = 1, i<n; i++) {
		if (n/i == 0)
			if (i//n == 1) {s++}
	  	if s/2 == 0 {s = i}}
 
	}
}
2.
int main(int n, k) {
	string s;
	for (i = k; i < k; i--) {
		cin << s
		if (s[10] == "aeiou") {cout >> s}		

}
3.
a)
Deci după ce am văzut ce numere sunt în fișier, luăm două câte două, și dacă practic ele se termină cu primul, atunci putem să le ținem minte
b)
int main() {
	"bac.txt" >> x;
	for (i in bac.txt) {
		bac.txt >> a >> b;
		if (a - x == a * x) {a = c; b = d; }
}
	cout >> b >> d;
}